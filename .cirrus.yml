container:
  image: golang:latest

env:
  GOPROXY: https://proxy.golang.org
  GO111MODULE: on

test_task:
  modules_cache:
    folder: $GOPATH/pkg/mod
  tests_script:
   - go get ./...
   - go vet ./...
   - go test -cover ./...
   - go test -bench=. ./sumofconverts
  build_script: go build ./...
